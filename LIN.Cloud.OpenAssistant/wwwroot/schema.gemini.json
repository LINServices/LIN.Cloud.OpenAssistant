{
  "type": "object",
  "properties": {
    "usertext": {
      "type": "string",
      "description": "Texto que le dir�s al usuario"
    },
    "commands": {
      "type": "array",
      "description": "Lista de comandos que se van a ejecutar en formato string",
      "items": {
        "type": "string"
      }
    },
    "actions": {
      "type": "array",
      "description": "Lista de acciones del servidor que se van a ejecutar solo en formato string",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "usertext",
    "commands",
    "actions"
  ]
}